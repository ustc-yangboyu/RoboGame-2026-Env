# RoboGame Environment

This is the environment configuration for the RoboGame contest.

## Quickstart

```bash
# Default (uses env_without_color.xml)
python test.py

# With custom XML and simulation speed
python test.py --xml env.xml --sim_speed 2.0
```